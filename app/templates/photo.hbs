<div class="Modal {{if model "is-open"}}">
  {{#link-to "photo" previousPhoto}}&larr;{{/link-to}}
  {{x-img img=model onClick=(action "goBack")}}
  {{#link-to "photo" nextPhoto}}&rarr;{{/link-to}}
</div>

{{outlet}}
